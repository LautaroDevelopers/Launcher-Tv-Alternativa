<resources>
    <style name="Theme.Launcher" parent="@style/Theme.Leanback">
        <!-- Estilo custom para los Headers de las filas -->
        <item name="rowHeaderStyle">@style/CustomRowHeaderStyle</item>
    </style>

    <!-- Estilo del Header de las filas -->
    <style name="CustomRowHeaderStyle" parent="Widget.Leanback.Header">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingTop">24dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>

    <!-- Theme para el panel de configuración -->
    <style name="Theme.Launcher.SettingsPanel" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@style/SettingsPanelAnimation</item>
    </style>

    <!-- Animación del panel (slide desde la derecha) -->
    <style name="SettingsPanelAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in_right</item>
        <item name="android:windowExitAnimation">@anim/slide_out_right</item>
    </style>

    <!-- Theme para el modal de permisos (obligatorio, sin poder cerrar) -->
    <style name="Theme.Launcher.PermissionsDialog" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.85</item>
    </style>
</resources>
